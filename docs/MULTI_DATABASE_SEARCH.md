# 多数据库检索使用指南

## 🎯 功能说明

论文检索助手支持从多个学术数据库同时检索论文：
- **arXiv** - 预印本（计算机、物理、数学）
- **PubMed** - 生物医学
- **ACM Digital Library** - 计算机科学
- **medRxiv/bioRxiv** - 预印本
- **IEEE/Scopus** - 需要 API token（可选）

---

## 📊 检索逻辑

### 问题：为什么之前只看到 arXiv 的结果？

**原因：** `limit` 参数限制了**总数量**，arXiv 先检索完成后就停止了。

**示例：**
```bash
python script\paper_research.py --query '[healthcare]' -l 10
```

1. arXiv 检索到 10 篇论文
2. 总数达到 10 篇，停止检索
3. **PubMed、ACM 等数据库被跳过**

### 解决方案：使用 `limit_per_database`

**新参数：** `--limit-per-database` 限制每个数据库的检索数量

**示例：**
```bash
python script\paper_research.py --query '[biology]' -l 30 --limit-per-database 10
```

检索逻辑：
1. arXiv: 检索 10 篇
2. PubMed: 检索 10 篇
3. ACM: 检索 10 篇
4. 其他数据库...
5. **总计最多 30 篇**（去重后可能更少）

---

## 🚀 使用方法

### 方法 1：命令行（推荐）

**基础检索（只检索 arXiv）：**
```bash
python script\paper_research.py --query '[machine learning]' -l 10
```

**多数据库检索（推荐）：**
```bash
# 每个数据库检索 10 篇，总共最多 50 篇
python script\paper_research.py --query '[healthcare]' -l 50 --limit-per-database 10

# 不限制每数据库数量（会检索更多）
python script\paper_research.py --query '[AI]' -l 100 --limit-per-database 50
```

**只检索特定数据库：**
```python
# 需要修改代码，在 script/paper_research.py 中添加：
# databases=['arxiv', 'pubmed']  # 只检索这两个
```

### 方法 2：Web 界面

**当前版本：**
- Web 界面默认行为：检索总数量后停止
- 主要用于 arXiv 快速检索

**即将更新：**
- 将添加"每数据库限制"选项
- 支持多数据库并行检索

---

## 📈 性能对比

| 检索方式 | 论文数 | 数据库 | 时间 |
|---------|--------|--------|------|
| `-l 10` | 10 | 通常只有 arXiv | ~30 秒 |
| `-l 50 --limit-per-database 10` | 最多 50 | arXiv + PubMed + ACM | ~3-5 分钟 |
| `-l 100 --limit-per-database 50` | 最多 100 | 全部数据库 | ~10-15 分钟 |

**注意：** 多数据库检索需要更长时间，因为要访问多个 API。

---

## 💡 最佳实践

### 场景 1：快速检索（arXiv 预印本）
```bash
python script\paper_research.py --query '[deep learning]' -l 20
```
- ✅ 快速（30 秒）
- ✅ 最新研究（预印本）
- ⚠️ 主要是计算机科学

### 场景 2：全面检索（包含医学文献）
```bash
python script\paper_research.py --query '[AI] AND [healthcare]' -l 50 --limit-per-database 20
```
- ✅ 多学科覆盖
- ✅ 包含同行评议论文
- ⏳ 需要更多时间（5 分钟）

### 场景 3：大量文献收集
```bash
python script\paper_research.py --query '[machine learning]' -l 200 --limit-per-database 100
```
- ✅ 大量论文
- ⏳ 需要较长时间（15 分钟）
- 💾 建议指定输出目录

---

## 🔍 查看结果

检索完成后，结果保存在：
```
papers/your_folder/
├── results.json    # JSON 格式的所有结果
├── pdfs/           # 下载的 PDF 文件
└── manifest.csv    # 论文清单（如果有）
```

**查看各数据库统计：**
```bash
# 查看 results.json 中的 "number_of_papers_by_database" 字段
```

---

## ⚙️ 高级选项

### 指定数据库列表

修改 `script/paper_research.py` Line 119：
```python
findpapers_search(
    query=self.query,
    outputpath=output_file,
    limit=self.limit,
    limit_per_database=self.limit_per_database,
    databases=['arxiv', 'pubmed'],  # 只检索这两个
)
```

### 设置日期范围

```python
# 在代码中添加 since 和 until 参数
search(
    query='[AI]',
    since=datetime.date(2023, 1, 1),  # 2023 年之后
    until=datetime.date(2024, 12, 31),  # 2024 年之前
    ...
)
```

---

## 🐛 常见问题

**Q: 为什么设置 limit_per_database 后还是很慢？**
A: PubMed 检索可能很慢（有 50 万+ 生物相关论文）。建议减少 `--limit-per-database` 的值。

**Q: 可以只检索 PubMed 吗？**
A: 需要修改代码添加 `databases=['pubmed']` 参数。

**Q: 为什么有些数据库返回 0 篇？**
A: 查询词可能不在该数据库的范围内。例如：
- `[quantum physics]` - arXiv 有很多，PubMed 可能很少
- `[clinical trial]` - PubMed 很多，arXiv 很少

**Q: 如何查看来自哪些数据库？**
A: 打开 `results.json`，查看每篇论文的 `selected_databases` 字段。

---

**最后更新：** 2026-02-22
